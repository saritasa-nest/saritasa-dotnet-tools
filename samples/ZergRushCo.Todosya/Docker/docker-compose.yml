version: '2.1'
services:
  db:
    image: microsoft/mssql-server-windows-express
    environment:
      sa_password: "29hHzGtB"
      ACCEPT_EULA: "Y"
    networks:
      - default
  web:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      - "db"
    ports:
      - "5001:80"
    
networks:
  default:
    external:
      name: nat
