version: '2.1'
services:
  zergdb:
    build:
      context: ./Docker
      dockerfile: Dockerfile.db
    image: zerg/db
    environment:
      sa_password: "29hHzGtB"
      ACCEPT_EULA: "Y"
  zergweb:
    build:
      context: ./Docker
      dockerfile: Dockerfile.web
    image: zerg/web
    depends_on:
      - "zergdb"
    ports:
      - "8080:80"
    volumes:
      - "temp:C:/Temp2"
    
networks:
  default:
    external:
      name: nat

volumes:
  temp:
