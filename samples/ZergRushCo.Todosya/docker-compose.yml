version: '2.1'
services:
  db:
    image: microsoft/mssql-server-windows-express
    environment:
      sa_password: "29hHzGtB"
      ACCEPT_EULA: "Y"
    networks:
      - default
  web:
    build:
      context: .
      dockerfile: ./Docker/Dockerfile
    depends_on:
      - "db"
    ports:
      - "5001:80"
    volumes:
      - "temp:C:/Temp2"
    
networks:
  default:
    external:
      name: nat

volumes:
  temp:

